name: Tests

on: workflow_dispatch

jobs:
  BashTest:
    runs-on: ubuntu-latest
    steps:
      - name: checkoutcode
        uses: actions/checkout@v2

      - name: first bash command
        run: |
          str = "Hello World"
          echo $str
      - name: try take prev bash command
        run: |
          echo $str
