name: Tests

on: workflow_dispatch

jobs:
  BashTest:
    runs-on: ubuntu-latest
    steps:
      - name: checkoutcode
        uses: actions/checkout@v2

      - name: Step 1
        id: step1
        run: |
          myArray=("jeden" "dwa")
          myArray+=("trzy" "cztery")
          echo "Hello, world!"
          echo "savedArray=$myArray" >> $GITHUB_ENV

      - name: Step 2
        run: |
          echo "The value of MY_VARIABLE is ${{ env.savedArray }}"
