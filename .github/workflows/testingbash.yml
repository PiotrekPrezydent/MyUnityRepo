name: Tests

on: workflow_dispatch
env:
  testArray: ()

jobs:
  BashTest:
    runs-on: ubuntu-latest
    steps:
      - name: checkoutcode
        uses: actions/checkout@v2

      - name: Step 1
        id: step1
        run: |
          myArray=("jeden" "dwa")
          myArray+=("trzy" "cztery")
          echo "Hello, world!"
          echo "echo "::save-state name=testArray::myArray"
          

      - name: Step 2
        run: |
          myArray=${{env.testArray}}
          for str in ${myArray[@]};do
            echo $str
          done
